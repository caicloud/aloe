summary: "Try get a product in nested context"
flow:
- description: "Get product inited in context.yaml"
  request:
    api: GET /products/%{product}
    headers:
      "Content-Type": "application/json"
  response:
    statusCode: 200
